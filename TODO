Things to fix/implement:

	ALL:
	 + Documentation
	 * More comments
	SYNGE:
	 + Variables (a = 1)
	 * Generalise helper functions
	 * Improve error reporting (give approx position)
	CLI:
	 + Getopt command line options for cli
	 + Add help page
	GTK:
	 * (Better) GTK+ interface
	TESTS:
	 * Add more tests
