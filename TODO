Things to fix/implement:

	ALL:
	 + Documentation
	 * More comments
	SYNGE:
	 + Functions (f(x,y)=x+y) -- maybe
	 + Multiple evalutations per line (a=1;a+2;a=a+1;a)
	 * Generalise helper functions
	 * Improve precision (try to fix rounding errors)
	CLI:
	 + Getopt command line options for cli
	 + Add help page
	GTK:
	 * (Better) GTK+ interface
	TESTS:
	 * Add more tests
