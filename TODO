Things to fix/implement:

  ALL:
   + Documentation
   * Replace as many hardcoded comparisons with structs
   * General code clean-up (especially tokenisation & evaluation code)

  SYNGE:
   + Add bignum support
   + Multiple evaluations per line (a=1;a+2;a=a+1;a)
   + Conditional expressions (exp ? if : else)
   + Make '=' an operator (to allow the use of variable setting inside an expression)
   + Functions to:
	- Get factors of a number
   * Refactor helper functions
   * Check operators using strings, not characters

  CLI:
   + Getopt command line options for cli
   + Add help page

  GTK:
   * (Better) GTK+ interface

  TESTS:
   * Add more tests
